import { default as React, ReactNode } from 'react';

interface ToastProviderProps {
    children: ReactNode;
}
declare const DesmyToastProvider: React.FC<ToastProviderProps>;
export { DesmyToastProvider };
